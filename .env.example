# Copy to .env and fill in. .env is gitignored — never commit real keys.
# Free key from https://dune.com/settings/api
DUNE_API_KEY=

# Optional: override the public RPC (only if the default rate-limits under traffic)
# RPC=
